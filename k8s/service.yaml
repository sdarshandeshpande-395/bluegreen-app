apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  selector:
    app: myapp
    version: blue   # 👈 traffic initially goes to BLUE
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30007
